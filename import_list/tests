let rubric_version = "1.0"
let rubric_title = "CS421 Fall 2017 Recursion Basic PatternMatching, List, pair_with_all"
(**************************************************************************
 * You can add new test cases by adding new elements to the following lists
 * Format is:
 * TEST<X>ARG(<weight>, <function_name>, <arg1>, <arg2>, ..., <argX>)
 *
 * <X> is the number of argument that the function being tested takes.
 **************************************************************************)



(* This list is for regular problems *)
let rubric =
[
  TEST1ARG(1, import_list, [(7,1);(4,2);(6,3)]);
  TEST1ARG(1, import_list, []);
  TEST1ARG(2, import_list, [(12,10000000);(0,-2);((-7),-10000000);(22,10000000);(0,2);(59,101000)])

]
(* Note: the last entry should not be followed by a semicolon. *)

let extra_rubric = 
[
]
